version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site deployment - no build required"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
